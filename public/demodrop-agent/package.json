{
  "name": "contentagent-demo-agent",
  "version": "1.0.0",
  "description": "contentagent.space — AI Demo Agent Netlify Function",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
